/**
 * MOCK FUNCTIONS: https://www.youtube.com/watch?v=sVX6LNiNAeA
 * Mocking is about replacing something that we don't control
 * with something we do
 *
 * Main goals:
 * 1. Capture calls
 * 2. Return custom value
 * 3. Change the implementation
 *
 * Mocking modules and functions:
 * - jest.fn() — mock a function
 * - jest.mock() - mock a whole module value
 * - jest.spyOn() - 'spy' or mock a function (like jest.fn()
 *  but without changing the implementation of the function)
 */
